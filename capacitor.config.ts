import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'cordova-plugin-swift-and-kotlin',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
